PORT=8899
JWT_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio

DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=cambia-esta-contrasena

COOKIE_SECURE=false
FORCE_LOGIN=true
CLIENT_EXE_MAX_MB=500

# Dominio o IP publica que los clientes RustDesk usaran para llegar a hbbr.
RUSTDESK_RELAY_HOST=192.168.0.4
RUSTDESK_SERVER_COMMIT=70dcfdeb95ccd848ff5dc64df69ff2da4131c5a8

# Solo se necesitan al probar el perfil opcional de MySQL.
MYSQL_PASSWORD=cambia-esta-contrasena
MYSQL_ROOT_PASSWORD=cambia-esta-contrasena-root

# Opcional: correo para alertas.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
